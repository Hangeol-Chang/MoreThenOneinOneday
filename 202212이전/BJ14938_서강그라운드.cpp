#include <iostream>

using namespace std;
/*
n개의 위치에 대해 모두 체크해야 함.
*/

struct vertex {
    int v;
    int w;
};

bool comparevertex(vertex a, vertex b) {
    
}


int main() {
    int n, m, r;
    cin >> n >> m >> r;

    int map[n];
    for(int i = 0; i < n; i++) {
        cin >> map[i];
    }

}